# Turek-Hron strict trace-space audit, 2026-09-05

## Explicit continuation and WIP publication, 2026-09-06

The user resumed work after the archived quota stop, explicitly withdrew the
10% stop threshold, and authorized committing and pushing all current project
work to GitHub before quota exhaustion. The earlier pause below is historical.
No quota-reset credit has been used. The current core is still the reviewed
`26f37eb1` candidate; its 25-test native verification and continuous physical
validation remain incomplete at this source checkpoint.

The next focused run will use a fresh output label and a manifest that records
this commit's actual execution identity while preserving source and input
hashes. The old A50 baseline and all original captured identities remain intact.
The full-replay geometry-binding review correction is being prepared externally.

Publication destination: [R15 WIP source and complete diagnostic evidence](https://github.com/lizhuoh9/EasyFsi/releases/tag/r15-wip-20260906-1330).
The 141,016,737-byte archived snapshot predates this explicit continuation;
its SHA256 is `671728300c42b7e0a0d50a9169d39ab50854b66ef53d2f9d808ac2c2fa15b5c7`.
It preserves the stopped WIP and original evidence without claiming a native,
continuous, component or formal benchmark pass.

## Earlier R15 quota-stop snapshot, 2026-09-06

The campaign is `PAUSED_AT_USER_QUOTA_BOUNDARY`. The root observed 10% main
quota remaining and stopped all owned test/numerical work under the user's
preserved stop/archive rule. No reset credit was redeemed; both existing credits
remain untouched. Resume requires an explicit user continuation.

The reviewed candidate is applied at core `26f37eb1`, with focused test mixin
`2423bdc6` and new R15 fixture `c4141569`. Its native verification is incomplete.
The frozen 25-test strict-CUDA/f32 regression was interrupted during first
compilation, before any case file or terminal test report was completed.
Owner PID/PGID400 received SIGINT and then SIGTERM; exit status was -15.
Exec32892 and the stop helper are closed. This interruption is an administrative
quota stop, not a numerical failure or a regression pass. No owned GPU job remains.

The original A50 R15 failure was reproduced at exactly four faces with unchanged
inputs and source: (x=0..3,y=50,z=342), axis2. Full readback verified18x153 native
snapshot arrays,207 original input arrays,193 source files, all eight atomic
publication fields and39 cleanup fields. The frozen replay advances no time.
Its readback report is `039f3270a52e408e56be740ab05e795dbcd7cc803fd493c37abac1062c9dc5c0`.

The isolated R04 native candidate query identified the cause of that rejection:
the first ordinary pair lacks a valid physical geometry proof, while five
alternative pairs among the four materialized candidates agree on one owner
and physical B/Q. The repair searches the candidate pairs only when the old
admission-and-full-valid cache is unavailable. It requires a unique consistent
trace, a seed containing a direct member and proof of every actually consumed
member. Ambiguous candidates retain rejection; original valid caches and the
zero/one-consumer behavior are preserved by contract. Two new temporary i32
fields participate in native cleanup, bringing the cleanup inventory to41.

The unchanged A50 consumer baseline completed2/2 strict-CUDA tests, with
1189.862763533 s native time and1230.101830959 s owned-process time.
Its16 exported canonical arrays were decoded and hash-checked, and source193,
host and runtime identities remained unchanged. Report:
`239766dd9e669da86573ac5749c8511dc9b07117e582e9e40354dd5258405839`.
This is the old-source comparison baseline; it does not validate the candidate.
Independent Astra/max reviews accepted the candidate core and focused tests
for controlled native validation; final launch guards9/9 and Ruff F/E9 passed.

The R12/R13/R14/R15 full-replay package completed host preparation only:
828 original arrays,1377 old-success snapshot arrays and983 immutable files
were verified. Its overall independent review is incomplete. A confirmed gap
remains in `success_adapter.py:115`: bind the replay's cached B/Q and related
geometry/target contracts to the existing frozen R04 query. Stable array hashes
alone do not prove correct geometry. The four native full replays were not started.

The latest physical result remains A50 R15: five accepted steps to0.025 s,
then a step6 rejection at trial time0.030 s. Accepted fluid and solid time were
each audited as0.025 s. Continuous success after this candidate, independent
complete rollback equality, the source-current coarse S0 operator check,
component qualification and formal FSI1/FSI2/FSI3 acceptance remain unverified.

After explicit resume, verify source/host/process/quota state; use a fresh
`native_green_r02` output and owner label for the unchanged25 focused contracts.
Do not reuse occupied `native_green_r01` or overwrite its manifest/evidence.
Then close the query-binding review gap, refresh package pins, run the four
frozen controls serially, and execute a fresh complete physical diagnostic.
Rebuild the original coarse operator from the R04 pose with4x48x288, auto112,
solid100,dt0.005,nine post-solid passes and external time0.040 s; keep original
targets and the1e-6 gate. Recompute source manifests before component/formal
qualification. Reduced field dumps and failed prefixes are not restart states.
No new repair commit or push was made.

Status: unresolved numerical discretization defects. No formal FSI1, FSI2 or
FSI3 pass exists. The user explicitly chose to retain the local 1e-6 m/s gate
and correct boundary/coupling representation. The diagnostic route retains
the original 112 physical marker positions, raw velocity targets and equations.
It does not smooth targets or discard original marker obligations.

Earlier A50 checkpoint, 2026-09-06: R15 on `a50b67f0` exited 1 after five accepted
steps to t = 0.025 s; step 6 failed during assembly 99 with four prepare-pair
conflicts. Its terminal audit passes as `FAILED_WITH_VALIDATED_ACCEPTED_PREFIX`,
with accepted fluid/solid time each 0.025 s and fresh8 false. Independent full
physical rollback equality and a source-current coarse S0 operator check remain
unverified. All 14 strict-CUDA contracts and repaired R12/R13/R14 full-domain
native controls retain their bounded passes. Earlier source identities,
fixed-coefficient obstructions and results retain their historical scope.

## Verified frozen baseline

Worktree: /home/zhuohengli/worktrees/HIBM-MPM-r25b-live.
Branch: codex/turek-hron-fsi123-validation-r26a.
Baseline HEAD: 5dcba9601d254f899488a44865f353c1a67497b3; no push.
Component source SHA:
9172c37ff7d2ecd61d6d412fd0642dbd287b5f3348a430fe048d418435247090.
Formal source SHA:
6fc496893ce4b4bf47b929c94ae378833e5ad9eb6ab1a8d177346e64ae4542e3.

All ten component stages completed at that source: three solid histories of
40 rows, two passing solid comparisons, fixed-fluid nx4/nx8 with 500 rows each
to t=2.5 s, their passing force comparison (relative delta 0.0063723541), and
coupled preflights of one/two accepted steps with complete fluid/solid macro time.
Those are component evidence for this baseline, not long coupled acceptance.
Subsequent component-identity changes require source-matched requalification.

Fresh formal turek_hron__fsi1_s0__5dcba96__r01 failed after seven accepted steps
to t=0.035 s. Step eight rejected hard-target closure at 5.37295090907719e-6 m/s
against 1e-6. Campaign failure/preflight and accepted-interface artifacts are in
validation_runs/turek_hron_fsi123_r26a/turek_hron__fsi1_s0__5dcba96__r01.
Use accepted_step_count=7, not a zero-based chunk filename to count steps.

## Why more iterations cannot repair the coarse case

Two observation-only eight-step replays reproduced closure call95 at step8.
The exact local system contains 320 hard-only adjustable rows, with no invalid,
immutable or projection-only rows. All 112 sample points coincide with their
physical markers. Raw legal-face and adjustable-mask mismatch counts are zero.

Independent minimax and dual certificates establish lower bounds:

| Component | Rows / columns / rank | Best possible max residual |
| --- | --- | --- |
| axis1 | 108 / 164 / 82 | 2.3042334e-6 m/s |
| axis2 | 104 / 162 / 81 | 3.3838747e-6 m/s |

The certificates have L1 norm1 and A-transpose times certificate equal to zero
in the assembled matrices. Witnesses involve markers50-53 near the clamp and
108-111 at the free cap. Representable manufactured targets on the same
supports solve below1e-18. This separates an insufficient trace space from
iteration exhaustion, illegal-face omission or a broken linear solver.

Collective F-only convergence uses its existing 1e-4 tolerance; the local
hard-only gate uses 1e-6. The collective result f_only_converged=true does not
authorize bypassing the local gate. Both checks rejected/accepted according to
their own contract, and canonical publication correctly failed closed.

## Fixed112 refinement diagnostics

The nonformal 4x64x384 / solid200 / dt0.005 trial retained explicit54 side
markers and4 cap markers. It passed current configuration spacing/CFL guards.
It nevertheless failed at initial static geometry, before its first accepted
step, with12 canonical prepare_pair_arbitration target conflicts. The first
face is (0,28,345), axis2, with claim_count3. The printed two authors belong to
segment52-53; one samples its interior and the other clamps to its endpoint.
The face itself lies inside the finite segment. Equal zero targets are not
sufficient provenance. The missing third author and cached route ownership
must be captured before diagnosing the exact rejection or changing admission.

Observation-only replay r04 captured the original coarse failure plus actual
particle x/F/v and the complete physical marker state. Its frozen geometry was
rebuilt using the genuine case geometry/volume/search/ledger construction, not
a resampled coarse obstacle mask on the fine mesh. The coarse reference uses
its captured canonical obstacle/velocity fields; the finer diagnostic rebuilds
geometry and stops at the first prospective closure, before pressure-topology
cleanup and canonical publication. Thus it is a geometry-level comparison,
not a replay of a complete fine-grid pressure/flow trajectory.

Both matrix audits retained bit-identical112 physical positions and targets,
336 active rows, valid direct samples, and materialized candidates as f32 for
re-evaluation by the original canonical sampling kernel:

| Geometry | Materialized all-row max residual | Interpretation |
| --- | --- | --- |
| Original 4x48x288 | 3.3838966829e-6 | Reproduces coarse obstruction |
| Frozen 4x64x384 | 1.8674483044e-6 | Still insufficient; not approved |

The refined hard-only axis2 has a singular value2.7958e-9: an unregularized
least-squares correction reaches971 m/s and fails the rounded audit. Dropping
that weak mode leaves residual2.83e-6. A full-row near-null relation couples
the lower side near its free end to cap markers108-111. Increasing rank through
vanishing weights is not a robust numerical representation. No such candidate
was committed to physical state or called a successful benchmark step.

The frozen all-row1e-6 check is a diagnostic stronger than the separate existing
free-row collective tolerance; neither formal acceptance tolerance is changed.
Actual local hard-only rounded failures remain independently measured.

## Evidence and recovery work

External evidence root (Windows-backed, durable across WSL restarts):
/mnt/c/Users/lizhu/.codex/visualizations/2026/09/05/01a06f3c-1b43-7841-ac96-ca57e7365e03/robustness_work.

- hard_closure_diagnostic_5dcba96__r01/r02: exact rows, minimax/dual proof,
  original-mask audit and two independent replays.
- hard_closure_diagnostic_5dcba96__r03: initial static refined-grid failure.
- hard_closure_diagnostic_5dcba96__r04: exact particle/marker state capture.
- frozen_trace_48x288__r01 and frozen_trace_64x384__r01: assembled rows,
  candidate arrays, canonical f32 audits and near-null mode analysis.
- failure_persistence_patch: generic exception persistence work and review;
  its test/review completion must be read from the latest handoff, not inferred.

The failure-persistence repair covers ordinary runtime errors and actual final
output failures, observed rollback outcomes, and atomic complete accepted-history
replacement. It addresses missing/duplicate evidence; it does not resolve the
numerical trace-space defect. Final validation is recorded separately.

Continue with bounded static ownership and frozen-target checks before another
coupled ramp. Any representation/config change must preserve the original
strict gate, geometry/force ownership, physical-time accounting, and f32 audit.
Then freeze/requalify source, run fresh formal S0, proceed through M0/M1 and
conditional F0, FSI2 and FSI3. Oracle/learning remains gated by benchmark quality.

At an observed main quota of10% remaining or less, stop owned work, archive an
exact handoff and stop. Inspect quota at stage boundaries; elapsed time and
silent stdout are not evidence of a stalled or paused process.

## Captured cache-precedence defect and durability validation

Diagnostic r05 retained numerical source bytes throughout its execution and
reproduced12 conflicts before any accepted step. It captured all four raw
author slots, actual route selectors and cache identities across assemblies:

| Assembly | Cached authors / kinds | Actual direct routes | Result |
| --- | --- | --- | --- |
| 001 | 11096,11097 / direct,direct | Both reach face(0,28,345) | Full proof,2 claims, no conflict |
| 002 | 11097,11481 / direct,shadow | Both original directs still reach same face | Earlier pair no longer matches cache;3 claims, conflict |

The added shadow comes from row(0,29,345) and is stored at row(0,28,345).
The same-storage candidate selection overwrites an otherwise valid routed
direct/direct pair. Its reserved shadow is then excluded from ordinary
redundant-shadow consumption. This explains prepare_pair_arbitration path0;
the later total count3 alone was insufficient to diagnose the cause.

The bounded candidate fix is to preserve a direct/direct cache when both actual
direct selectors route to the target face. The same-storage alternative remains
available when that condition does not hold. Existing finite-segment union,
normal, anchor, search and shadow-provenance checks must remain unchanged.
At this record update the geometry fix is staged/under verification, not a pass.

The independent failure-persistence repair is applied in the case adapter and
reviewed by Astra/max. It records actual runtime/final-output phase and observed
rollback outcome. Both normal final CSV output and failure recovery atomically
replace complete accepted history, preserving existing bytes on partial/replace
failure. Tests include stale positive append cursors, actual per-row partial
writes, failed recovery, failed final summary publication and unavailable stderr.

- RED:18 expected focused failures on unchanged case code.
- GREEN:19 focused CPU tests, plus27 adjacent tests and13 subtests.
- Latest changed regression/module was rerun:19 passed in2.30 s.
- Ruff E9/F, compilation, diff checks and reverse patch applicability passed.
- Reviewed case SHA:d311c5b6befdba00e397632670f8e264dd3ea1e09f71992ccddbf259d4861523.
- Reviewed test SHA:d6ac1ec8a140a1dfec74d33f05f60865c5c66afc4d39a09ed8cec718e311279f.
- External patch SHA:7cb7763ba0d66359c4f785926e7f70129b7899a152cfe11e3ddd1f82bf71ddf3.

r05 also exercised the actual numerical exception path after this repair. The
case failure JSON now records evaluate_trial, failed step1/time0.005, zero
accepted steps, successful physical rollback and no persistence error. The
flushed-count scope is explicit: a zero count after failed recovery does not
assert that a preserved old file was empty.

The existing generic driver's combined nested-cause/rollback-failure chaining
is unchanged. Persistence preserves the exception chain it receives; it does
not claim to recover a nested cause already replaced by that driver.

This repair changes failure evidence handling only. It is not the numerical
trace-space repair or a formal S0 pass. No commit/push was made for this patch.

## Frozen96x400 completion and endpoint evidence boundary

The4x96x400 frozen-state diagnostic ended with8 canonical target conflicts
before matrix capture. It retained the same112 physical target values and
positions and advanced no physical time. Exact source/runtime identity and
the original traceback are saved under frozen_trace_96x400__r01.

The first conflict is face(0,43,305), axis2, path2, with2 claims from rows
key17504/17505 and physical segment0/1. These markers are near the lower free
tip (z approximately1.903/1.910 m); the clamp is atz2.25 m. The target face at
z1.90625 m lies inside the finite segment. One author projection clamps to
marker0 and the other is interior. The observed conflict therefore does not,
by itself, prove a missing-corner-support rejection. Its reconstruction and
route certificates must be diagnosed independently of the endpoint design.

A separate geometric limitation is established from the current construction:
nominal half-cell extension at96x400 is3.125 mm along the side and2.13542 mm
along the cap, smaller than their physical quadrature half gaps3.24074 mm and
2.5 mm. Explicit endpoint coverage is under design review; it has not yet been
implemented or verified as the remedy for this conflict. No physical marker
may be removed, resampled, or assigned a smoothed target to pass the frozen
comparison. The original local1e-6 m/s gate remains unchanged.

At this boundary, the pair-cache regression tests and one-predicate correction
are entering focused verification. The numerical source is dirty; baseline
component/formal identities identify prior evidence, not the current source.

## Whole-campaign representation consumers

The solver-free matrix is saved as registered_representation_audit.json.
All registered refinements use automatic physical marker spacing near0.75
of a fluid-cell width. Refining the grid while retaining that automatic
density repeats the representability risk identified at S0; this is a
risk inventory, not a numerical failure certificate for every stage.

Any selected representation must therefore update the complete stage
matrix coherently after exact-state validation. Explicit marker counts
also require expected_stage_marker_count to honor the registered values;
its present implementation always computes automatic counts.

Periodic telemetry indexes the physical accepted marker positions using
the physical segment list. Six projection-only endpoints must not be
passed to that physical-only array consumer. This is included in the
endpoint implementation review before any long periodic run.

FSI2/3 intentionally use relative-only coupling acceptance, unlike S0.
Keep that contract intact and explicitly exercise its near-zero start.
No formal stage specification or acceptance gate was changed by this audit.

## Final authorized regression and user pause

The cache repair completed strict-CUDA GREEN:9tests in198.993s, no failures,
errors or skips; exec86106 exited0. Its RED reproduced2static/moving subtest
errors on unchanged core in552.209s. The initial zero-test CPU setup failure
is not RED evidence. Astra/max accepted the scoped patch and regression design.
No tests were adjusted to obtain GREEN. Full112 replay was not started.

The user then stopped work at this requested boundary. Endpoint staging is
unreviewed/untested/unapplied WIP. All owned test/numerical jobs are stopped;
no commit/push or automatic resume. Last observed main quota:20%remaining.
See [the final handoff](../refactoring/TUREK_HRON_FSI123_TRACE_REPAIR_THREAD_HANDOFF_2026-09-05.md).

## Frozen96x400 r02 route-proof replay and endpoint staging review

The never-used frozen_trace_96x400__r02 label replayed the exact r04 frozen
state (source npz sha256 f4bb7e1bf9639f7b6c20c149063edb5e99b994d61ff906ec3f4d245493bf3211)
on 4x96x400 under the full arbitration observer. The precommit conflict
reproduced identically to r01: count=8, first face(0,43,305), axis2, path2,
claim_count2, authors 17504/17505 from source rows (0,43,304)/(0,43,305) near
the lower free tip. The captured payload adds the concrete cause label
conflict_source=segment_reconstruction_invalid: the two adjacent rows
reconstruct different axis-1 targets, 0.0009648114792071283 versus
0.0010178317315876484 m/s, from physical markers 0 and 1 (raw marker
velocities 0.0009648114792071283 and 0.0010208557359874249). Observer
snapshots cover segment-pair precompute, claim prepare, merge audit and both
precommit boundaries with zero observation errors; source bytes were verified
unchanged during the run. Matrix capture was not reached, so this run yields
no feasibility result, and the 80x384 replay remains unstarted.

The archived endpoint staging was then reviewed read-only. Its three baselines
match current production exactly (core feed249834d6..., interface_state
9276ee1348..., case d311c5b6...), the staged patch SHA verifies as 67fa0568...,
dry-run and scratch-tree apply are clean, and all four staged files
byte-compile. A scratch CPU run of the staged solver-free tests passed the
112/118 topology contract and failed test_periodic_telemetry_keeps_the_
physical_112_row_topology with KeyError marker_region_id: the staged test
hands the existing _marker_spacing helper a record without marker_region_id
and marker_order. That is a staged-test contract mismatch for the endpoint
author to fix at review time; production code was not modified anywhere and
no fix is claimed here.


## Resumed live work: registered same-segment storage, 2026-09-05

The user explicitly resumed and authorized scoped WSL elevation. The exact
worktree, branch and HEAD above were reverified live with CPython 3.10.12,
NumPy 2.1.2, SciPy 1.15.3 and Taichi 1.7.4. The earlier user-pause section is
historical. No commit or push has occurred during this resumed work.

Observation-only frozen96 r03 called the actual CUDA sampler and both storage
selectors. The accepted sample equaled the nominal probe. The generic selector
chose (0,43,304), while the pair selector chose shared face (0,43,305); both
reported valid. The two serialized authors passed provenance checks and the
three neighboring obstacle entries were zero. This establishes the stored-face
selection mismatch at the axis2 reconstruction witness.

The repair selects the existing pair result only for a proved registered
same-segment direct pair. It preserves the existing bridge route, requires the
ordered primitive exactly once with sentinel -1, and retains selected-face,
finite-value, alpha, ownership and final publication checks. The captured
nonzero-target CUDA regression first reproduced the original rejection. A
separate malformed-sentinel RED exposed the missing sentinel check before it
was corrected. GREEN ran 10 tests in 440.192 s with no failures or errors,
including reverse, malformed and duplicate topology mutations and adjacent
valid/fail-closed paths. No raw physical position or velocity target changed.

Verified GREEN source SHA256:

- core.py: de8b94abcc3666ecdd7547c550545971326a3bd503e00a589a98c50cb9043bf3
- _hibm_component_face_ledger_contracts.py: 99c883441d6954126f417c003798bd7559741b727e5179694e73e5593df49097

Frozen96 r04/r05 retained four prepare_pair_arbitration conflicts and removed
the four axis2 reconstruction conflicts. The remaining first face is
(0,50,336), axis1, with direct authors 19936/20336 on physical segment84/85.
Those four prepare conflicts were already present in the r03 prepare snapshot;
they were not introduced by the storage repair. Both current captures verified
unchanged source bytes and zero observation errors. Neither reached the matrix,
so no new feasibility result exists. The r05 observer incorrectly populated
face_center_m[1] from cell_face_z_m; that coordinate is invalid diagnostic data.
Its author fields, route offsets and pair caches are unaffected. Corrected
axis-aware r06 added private counters to an exact copy of the active CUDA
admission helper. It reproduced the same four conflicts; only the strict
same-segment bracket rejection fired (source line16472). Author weights differ
by 3.04e-6 while each side of the bracket has only about 1.5e-6 clearance, below
the existing 2e-6 parameter margin. Both stored anchors are identical and only
1.69213e-8 m from the canonical point, inside the existing 5.01673e-7 m geometry
tolerance. A discriminating regression is being prepared before any correction.

The original endpoint draft was rejected for stale feedback refresh, rollback
after retirement and bridge reversal/overlap defects. Repaired external staging
passed 96 CPU/mock checks and independent Astra/max source review. It retains
112/223/334 physical rows and adds six private zero-area/zero-force vertices;
30 collected CUDA cases remain unexecuted. Persistent-checkpoint consumer
support is being prepared separately. Staging has not changed production and
does not establish physical closure feasibility or formal benchmark acceptance.

Evidence root (external, append-only run labels):
C:/Users/lizhu/.codex/visualizations/2026/09/05/01a06f3c-1b43-7841-ac96-ca57e7365e03/robustness_work/
The focused logs are under resume_live_20260905_01; frozen captures are under
frozen_trace_96x400__r03 through r05. Continue with original local 1e-6 m/s
and full fluid/solid accepted macro-time obligations. Recompute the component
identity before deciding which component evidence needs requalification.


## Frozen96 r07: full physical-row feasibility

The coincident-anchor repair reuses the existing geometric coincidence bound
before choosing the strict parameter bracket. It changes no tolerance, raw
target, normal, search envelope or final ownership check. RED ran two actual
CUDA tests in19.671s: the captured valid pair reproduced the prepare conflict,
while the shifted-anchor atomic-rejection test passed. GREEN ran10tests in
236.445s, all passing, including strict brackets, coincident probes, zero/one-sided
progress, disjoint topology, relocation transport and the earlier selector fix.

Current tested core SHA256:
244f5372ece21ad8608e5f617fa308f9a8bf89e396ad0cf9179cc9b9a5fc6f2c.
Current tested ledger-contract test SHA256:
bde388e8f8feb6097b22d65a872b4b0344e0eaedd36487d6284ac3143d16c77b.
E9/F and git diff --check passed. Full Ruff reports five pre-existing style
findings also present in the exact prior source: one E711 and four E702.

The never-used frozen_trace_96x400__r07 replay used the original production
helper, with an axis-correct observational capture. It completed geometry and
matrix collection in198.349s, with unchanged source bytes and no observer
errors. All112 physical positions, velocities and sampling positions remained
bit-identical to the frozen input. All336 physical rows were active and valid.

| Axis | Full rows / columns / rank | Smallest singular value |
| --- | --- | --- |
| 0 | 112 / 118 / 112 | 0.2669531139 |
| 1 | 112 / 234 / 112 | 0.1887643676 |
| 2 | 112 / 232 / 112 | 0.1888360491 |

The materialized f32 candidate, re-evaluated by the original canonical kernel,
has maximum physical-row residual2.4616284011180767e-10m/s, below the unchanged
1e-6m/s requirement. Maximum candidate velocity is0.00495626684m/s; maximum
velocity correction is0.00342810201m/s. This removes the earlier rank/near-null
obstruction for this exact geometry without discarding or smoothing targets.
It does not establish pressure-closure convergence, time integration, long-time
benchmark accuracy or a pass for every future deformed geometry.

Next is hard_closure_diagnostic_5dcba96__r06: an observed, from-zero8step CUDA
run on4x96x400, fixed54side/4cap markers, dt0.005s and200solid substeps. It keeps
the original candidate numerical-health validator and records accepted steps
and failures. No endpoint patch or formal stage-matrix change is included.

### Endpoint contingency remains outside production

The optional accepted-checkpoint consumer passed108CPU/mock tests plus2subtests,
but independent review rejected it. A finite saved root owner placed at its
fixed endpoint passes structural/layout checks; actual endpoint refresh then
rejects the collapsed bridge after macro restore has written solid/fluid state.
Before that candidate can be used, saved geometry needs a pure-array preflight
for collapse, signed bridges and cap order, shared by checkpoint validation and
marker-state prevalidation before every owner write. Negative tests must assert
unchanged checkpoint/journal bytes and zero runtime owner writes. Existing
layout-identity prevalidation is correct but insufficient. The refresh mock in
the current passing tests does not expose this geometric failure.

Static integration preparation may prove patch applicability; it does not clear
this blocker. Its test fixture import needs tests.cases instead of tests.coupling.
The staged case's side_count>=2 guard also contradicts its eight-distinct-source
layout, whose minimum is3side/2cap. Current campaign counts are unaffected.
All endpoint and checkpoint changes remain external and unintegrated while the
current simpler production representation undergoes actual coupled validation.


## Fresh96 r06: rejected step and exact matrix audit

The from-zero fixed-112 diagnostic completed after 474.578 s with exit 1.
It accepted zero steps and failed on closure call 8 during the first trial at
t=0.005 s. The hard residual was 1.842820324782224e-6 m/s against the unchanged
1e-6 m/s closure tolerance; invalid count and failure code were both zero.
Source bytes matched before and after the run. No arbitration conflict occurred.
The case failure artifact records physical_state_restored=true, rollback_outcome
restored, and fluid/solid/marker_interface/marker_pressure_gradient restoration.
Derived search/boundary state requires rebuild. This rejected trial contributes
no accepted physical time; the failure snapshot is not a restart checkpoint.

The strict-CUDA actual_matrix_audit_r01 completed in 42.913 s without advancing
physical time. It used failed_closure_inputs.npz SHA256
0f163f7aa8c8fe0eb07140fd0bf513120e4880931b9a1a4e28540a9934cceb87.
All 336 rows were active, including 311 hard-only rows. Raw targets were unchanged;
valid-mask and adjustable-mask reconstruction each had zero mismatches. Of 112
sample locations, 79 were direct and 33 used the existing normal-walk fallback;
none used nearest-cell fallback. Maximum walk distance was 0.002135425806 m.

Axis-2 hard rank was 82/91 and all-row rank was 103/112. Markers 84/85 sampled
the same hard z-face degrees of freedom, although their physical positions and
raw velocities differ. A minimax solve and independently checked L1-normalized
dual witness agreed on 9.21410124664075e-7 m/s. The original CUDA row evaluation
of the materialized f32 all-physical candidate measured 9.21410560295044e-7 m/s;
maximum correction was 1.5484222e-5 m/s. Thus this state has a candidate within
the registered hard gate, despite exact rank deficiency. This is diagnostic
feasibility evidence, not closure publication, pressure acceptance or time advance.

The failure traceback places call 8 immediately after the first post-solid
solid-band conversion, before the later outlet-reachability and row-cloud/tiny
cleanup. Existing arbitration snapshots cover only the surviving k336 pair and
cannot establish when neighboring j49 support disappeared. A new observation
must compare full/local obstacle and ownership fields before and after that
conversion. In parallel, independent review found that the initial coincidence
guard skips an established direct-owner/shadow provenance flag. The mirrored
positive and corrupted-storage negative regressions are running before that
minimal branch-preservation correction. Neither the topology cause nor the effect
of this flag on fresh96 is established by the current evidence.


## Direct-author coincidence bound and provenance preservation

Independent review identified a compatibility regression in the initial
coincidence guard: bypassing the old parameter branch also suppressed the
direct-owner/shadow flag. The mirrored strict-CUDA RED reproduced both missing
flags and failure to reject corrupted shadow storage (2 methods, 4 subcase
failures, 542.522 s). Restoring the old branch and flag assignment passed the
first 14-test GREEN in 237.555 s, but that was not final acceptance.

Further review found a newly admitted mixed pair whose two physical anchors
round to the owner while its direct parameter falls outside the old owner bound.
It also found that missing registered membership resets a mixed geometry slot
to -1, so checking the slot alone cannot prove two direct authors. The second
RED took 427.376 s: both shifted-parameter cases wrongly raised no error;
both missing-registration cases exposed admission/full/flag=(1,1,0), although
the latter assemblies were rejected later at prepare arbitration.

The final patch passes one explicit author-kind predicate from the sole union
helper caller. Only the new coincidence fallback requires both authors to be
direct. The established strict parameter bracket, mixed direct-owner flag,
low-separation relocation transport and dedicated transverse-shadow path keep
their original checks. No numerical tolerance, physical marker, raw target,
search envelope or canonical publication contract changed.

Final source SHA256: cda0eca0c0d7ca06a20ffc0bc8605b688a46511d768bde07efad268840723f63
Final ledger-test SHA256: 9a9cb6d83b5bd4d0b96516521f89e5e0499a304de60540ce92c6056e14812715
The complete mixed_bound_green_r01 strict-CUDA suite passed 16 tests in
248.681 s, including the four new mirrored methods, both original cap tests,
the actual frozen direct/direct pair, topology corruption and adjacent rejection
contracts. This is focused implementation evidence; formal numerical gates have
not been requalified on this source.

The prepared hard_closure_diagnostic_5dcba96__r07 retains the prior eight-step,
fixed-112, 4x96x400, dt=0.005 s, 200-solid-substep settings and original candidate
health validator. Its additional observer captures the local i=0:4, j=47:52,
k=332:341 window around volume commit, search/carving, ledger construction and
solid-band conversion, plus actual prospective/direct/fallback closure sampling.
The observer chains the original calls, changes no solver field, and records
observation errors explicitly. Runtime topology and closure conclusions remain
pending that probe.


## Current-source r07 topology and exact zero-free witness

The fixed-112, 4x96x400 from-zero eight-step diagnostic r07 completed in
700.890 s with exit 1, zero accepted steps,
and rejection of physical step 1 at 0.005 s, closure call 8. Its hard residual
was 1.842820324782224e-6 m/s against 1e-6, with invalid count 0 and failure code 0.
Failure persistence confirms restoration of fluid, solid, marker interface and
marker pressure-gradient state. Derived search/boundary state requires rebuild;
the failed snapshot is diagnostic input, not a restart checkpoint.

The 83 observer snapshots cover 8 assemblies and 4 solid-band passes, with no
observer error. Post-solid dynamic/carve snapshots 0063-0065 retain j49,
k334:338 as fluid. Solid-band snapshots 0074-0075 change that pattern from
[0,0,0,0,0] to [1,0,0,1,1]; closure 8 retains it. The unchanged EXTERNAL search
classification and positive signed distance alone do not establish a topology
violation. Source bytes before/after the trajectory are identical: core
cda0eca0c0d7ca06a20ffc0bc8605b688a46511d768bde07efad268840723f63 and MAC
e50d84e1b6f92e51f6e6ce4534e9429713b0d690ac35308b01a0d006b89334ba.

Some velocity/deformation-gradient bytes differ from prior r06, so feasibility
was rebuilt from r07 rather than transferred by assumption. The strict-CUDA f32
actual operator audit took 4.326 s
and preserved source identity. It contains 336 active physical rows and exactly
311 rows with no nonzero weighted free support. Raw physical velocity targets
are unchanged, every sample is valid, and both valid/adjustable masks match the
actual claims and fluid-fluid topology exactly. Prepared sampling is distinct
from raw physical positions for the existing fallback rows; it is not a claim
that all 112 samples remain direct.

Rows 254 and 257 (physical markers 84/85, z component) share exactly two owned
hard DOFs, (1,49,336) and (2,49,336), with weights 0.5/0.5 and no free DOF.
Their captured RHS values are 1.842820324782224e-6 and
4.764324243282381e-13 m/s. A dual vector with coefficients +0.5/-0.5 has L1 norm
1 and exact matrix annihilation, giving a lower bound 9.214099241748998e-7 m/s.
The actual materialized f32 candidate reaches 9.214099350174365e-7 on hard-only
rows while all physical rows remain at most 1.4457244105869904e-5. Thus this
capture is feasible under the unchanged hard 1e-6 and absolute 1e-4 tolerances.

The public collective closure first accepts its global residual
1.4457244105869904e-5 at the separate absolute tolerance 1e-4, before checking
the owned-hard requirement. Existing proportional-F certificates require a
nonzero free pivot and cannot certify rows 239/254, the two exact zero-free
rows whose hard defects exceed 1e-6. The next bounded repair will add their
exact single-row witness and reuse the existing connected FH solve, device
residual audit and full caller re-audit before canonical publication. It must
preserve nonzero-F behavior, immutable ownership, raw targets, rollback and both
tolerances. Implementation/regression and fresh trajectory evidence remain
pending; neither this audit nor the earlier 16-test suite is a formal pass.

Evidence: external robustness_work/hard_closure_diagnostic_5dcba96__r07,
actual_matrix_audit_r01/actual_feasibility.json and zero_free_witness.json.
Captured input SHA256: 7a1cce29e6a19d45eb45fc8d864f0abcb27377cb1681e0568ccf0f09fcee7b7b.


### Tangential support transition before the rejected commit

Independent read-only inspection of r07 snapshots 0068 and 0078 confirms that,
for i=1/2 at j49, the z-face claim counts at k335/336/337 change from [1,0,2]
to [0,2,0]. The two-author route uses FACE_FIRST_FINITE_SEGMENT_PAIR (mode 4).
The captured band conversion gives those faces obstacle/fluid, fluid/fluid,
and fluid/obstacle neighbors respectively. Captured search rays have exactly
zero z component. The existing storage selector therefore excludes the two
mixed faces for this tangential component; source rows 335/336 retain fluid
cells and share face 336, while source row 337 loses direct eligibility.
This follows the existing regression
test_axis_aligned_direct_author_keeps_tangential_lane_off_obstacle_face.
The exact author keys and offsets were reconstructed from the unchanged
cda0eca0 selector and saved geometry; their z-face cache fields were not saved
by this observer. The count, mode, geometry and topology values are observed.

The loss is prospective: snapshot 0080 still has canonical masks [5,1,5] at
faces 335/336/337, while prospective valid/adjustable masks are [1,5,0]. The
prospective builder removes prior owned components and builds the next support
from current claims and fluid/fluid topology. Assembly 8 fails before canonical
commit. Existing wall-flux cleanup and normal-ray relocation contracts do not
retain these excluded tangential faces. This explains the implemented behavior;
it does not establish benchmark accuracy or justify a numerical gate change.


## Exact zero-free certificate repair and actual-input replay

The MAC collective closure now checks the exact single-row witness J_F[row]=0
before either F-only success return. A nonzero weighted free coefficient vetoes
that witness, regardless of mobility magnitude. Authorization requires a finite
RHS defect above the unchanged 1e-6 m/s owned-H tolerance and finite, nonzero
weighted owned-H support with finite positive mobility. Zero-weight support
flags do not confer authority. The existing proportional-F certificate retains
its separate 1e-4 absolute tolerance, and the two certificate families are
combined before the existing connected FH solve, device audit and application.
The caller still re-audits all physical and projection-only rows before commit.

The final implementation passed all 19 selected strict-CUDA f32 tests in
1650.286 s: nine new zero-free/weighted-authority tests and ten adjacent tests
covering identity, F-only minimax failure, nonfinite residuals, cleanup, existing
rank defects, projection-only re-audit and external ownership. The corrected
public-row fixtures exhibited the expected numerical failures before the
implementation; the initial MagicMock kernel-observer run was invalid as
numerical RED and is retained only as fixture-debugging history. One first-case
coordinate was subsequently changed to a binary-exact value before its valid
RED, without changing its tolerance or assertions.

The identical r07 capture (7a1cce29e6a19d45eb45fc8d864f0abcb27377cb1681e0568ccf0f09fcee7b7b) was then replayed through
the public collective interface, with zero physical steps. It changed the prior
closed=false/certificate_count=0 result to closed=true/certificate_count=2,
repairing four owned hard-target DOFs. The maximum hard-target delta was
9.214103897647874e-07 m/s. Fresh canonical
row reconstruction measured hard residual 9.214099350174365e-07
and all-physical residual 1.44572441058699e-05 m/s,
within the unchanged 1e-6 and 1e-4 gates. Every captured immutable field is
byte-identical, ownership violation counts are [0,0,0], and public-close
temporary rows/certificates/deltas are retired. The internal FH result's
global residual includes its private F candidate; the canonical post-application
measurement above is authoritative for this H-only public replay.

The actual replay completed in 87.705 s.
Core source remained cda0eca0c0d7ca06a20ffc0bc8605b688a46511d768bde07efad268840723f63;
MAC source was cc5d2601ac4d016ee6c6f8da141740fe15d8f215fe7f092804592a724a746947;
test source was 0257779563fe980a4438855b179cb6deef680bca68f9d236fb20c80f0a414ffc. Runtime requested and verified CUDA/f32 with
Taichi 1.7.4 and the selected Python 3.10.12 environment. Compile, Ruff for the
changed MAC/test files, and git diff --check passed. Existing unrelated core
Ruff findings remain outside that claim.

An independent Astra/max review accepted this scoped implementation after
inspecting the exact source, all 19 GREEN tests and the actual-input replay.
It reported no actionable findings. This is a code/evidence review verdict;
physical trajectory and formal numerical acceptance remain separate gates.

Evidence is in external robustness_work/resume_live_20260905_01:
zero_free_green_r01.json/.log, zero_free_certificate_20260906_01, and
actual_collective_green_r01.json/.log. Actual-input reports are under
hard_closure_diagnostic_5dcba96__r07/collective_replay_red_r01 and
collective_replay_green_r01. These focused results do not replace fresh physical
advancement, component requalification, the formal clean-source gate or any
registered FSI1/2/3 acceptance criterion. The 4x96x400 fixed112 grid remains a
diagnostic representation; the registered 13-stage matrix is unchanged.


## R08 diagnostic stop for acceptance-configuration propagation

R08 was deliberately interrupted before its first accepted step, after static
inspection found that the diagnostic uses 200 solid substeps but still calls
the registered S0 acceptance object with expected_solid_substeps=100.
The inherited expected_steps=1600 would also misrepresent a complete eight-step
assessment. Its other current identity expectations (dt .005, fluid substeps1,
physical markers112) agree with this diagnostic.

The owned process exited -2 after scoped SIGINT during Taichi compilation.
It produced five observer snapshots with no observer errors, and source bytes
before/after are identical. No accepted step was observed. No case rollback
result was emitted, so physical rollback is not claimed for this interruption.
The original process.json can retain its pre-interruption running value;
the owned job status and diagnostic_stop_observed.json record completion.

This is a diagnostic harness issue, not a numerical failure or a user pause.
The 19-test GREEN and actual r07 public replay remain valid. Earlier r06/r07
numerical failures occurred before this candidate acceptance check and are not
reclassified by the mismatch. The next diagnostic must derive its expected
identity fields from the actual final case configuration, preserve all numerical
thresholds and policy, and retain accepted rows per step for interruption-safe
evidence. No formal stage definition or solver source is changed by this repair.


## R09 diagnostic identity repair and paired accepted evidence

The external r09 adapter derives expected steps, dt, fluid predictor substeps,
solid substeps and resolved physical marker count from the actual case
configuration. For this diagnostic the only differences from registered S0
are 1600 to 8 expected steps and 100 to 200 expected solid substeps. Every
numerical threshold and the FSI1 numerical-health policy remain identical.
Actual-case preflight ran the current physical/grid/coupling validators with
Taichi initialization explicitly blocked. The 112-marker count and .005 s dt
match; the grid-derived MPM support radius recomputes to the existing .01 m.

The native accepted-interface record does not include both subsystem macro
time reports. R09 therefore snapshots the unchanged candidate/history row
only after its original validator returns successfully, then pairs it by exact
step/time with the native accepted callback after physical commit. Per-step
JSONL preserves that candidate row beside the full typed interface/work record.
Rejected or merely validated candidates are not published as accepted.
Candidate pending state and temporary factory/case overrides retire on run exit.
Capture errors are reported separately from numerical outcomes.

All 29 pure-host tests passed in 1.03 s; their import guard excluded Taichi,
case and solver modules. The separate actual-case preflight imported definitions
but verified that no Taichi runtime was initialized. Compile and Ruff F,E9
checks passed. An independent Astra/max review accepted the final adapter,
its corrected run-exit cleanup, tests and actual-config preflight, with no
remaining actionable findings.

Final adapter SHA256:
4c66fd071c9d810d1aac88bd660030d07538fbde2eb61a1531c5ebdc69b5c28d.
Wrapper: 25201c272f7a218079356dd0bacd26620cd0d4c8c235c7f8fb15af6e504f2644.
Tests: 8bd3390ca687fbbf20fabc609c77e70db133a4682e9454d3e7a74038135a15b8.
Evidence is in external resume_live_20260905_01/r09_cpu_r03.xml,
r09_adapter_verification.json, fresh96_r09_preflight_r02.json and
R09_DIAGNOSTIC_ADAPTER_REPORT_2026-09-06.md.

A new from-zero eight-step r09 run has been launched on the unchanged reviewed
MAC/core source. Its live diagnostic_acceptance.json matches the preflight
exactly. Physical advancement is pending; these host checks and configuration
records are diagnostic evidence and do not establish formal benchmark acceptance.


## R09 accepted prefix and certified step-2 infeasibility

The reviewed external r09 harness completed with exit 1. It accepted step 1 at
t=.005 s and failed at step 2, closure call 19, in evaluate_trial. The source
and observer captures remained unchanged and reported no capture errors.
The external output is hard_closure_diagnostic_5dcba96__r09.

The accepted JSONL contains one validated candidate paired with 34 typed native
callback arrays. The formal observer's helpers validate the marker-layout hash
and runtime metadata before the remaining 32 arrays enter AcceptedRecordValidator.
The candidate passes the actual diagnostic AcceptanceConfig and unchanged FSI1
numerical-health policy. Every one of the 89 persisted CSV fields matches it.
Both subsystem requested/accepted times are .005 s; remaining times are zero.
Fluid predictor substeps=1, solid substeps=200. Work counters record one trial,
zero rejected trials, one fluid solve, one solid macro solve, 200 MPM substeps,
384 pressure CG iterations and 390 pressure matvecs.

This proves the single accepted macro-time contract. It does not exercise live
multi-trial rejection accounting and excludes unaccepted step-2 work. The
failure artifact reports physical_state_restored=true, rollback_outcome=restored,
rollback_failure=null and derived caches requiring rebuild. That is a runtime
report, not an independent full-state before/after rollback comparison.
The first CPU audit used the core record schema on the full callback payload
and correctly rejected two metadata extras; the r02 audit follows the actual
formal observer's explicit metadata validation/separation and passes. Both
audits are preserved in accepted_time_audit_r01 and accepted_time_audit_r02.
The primary ran the CPU script supplied by the independent reviewer.

The new failure snapshot SHA256 is
9d87ca5cd6075bfd5302c5ae1f209023f71bbba314e08f5e00064ad413193869.
Actual CUDA reconstruction yields 336 active physical rows, including 311
hard-only rows, and no legal-mask or owned-adjustable-mask mismatch. Raw targets
are identical to captured physical targets. Some other markers use the existing
sampling fallback, but witness markers 84 and 85 remain at their raw positions.
Their z rows use exactly the same two owned H faces, (1,49,336) and (2,49,336),
with weights .5/.5 and no nonzero F support. The two raw target velocities differ.

| r09 z system | Rows / columns / rank | Best max residual |
| --- | --- | --- |
| Hard-only | 91 / 208 / 82 | 2.184912935710554e-6 m/s |
| All physical | 112 / 232 / 103 | 2.184912935710554e-6 m/s |

The hard-row dual has coefficients +.5 and -.5 on markers 84/85, L1 norm 1,
and exactly zero A-transpose-times-dual in the assembled matrix. Its positive
right-hand-side product certifies the stated lower bound, above the unchanged
1e-6 hard tolerance. Materializing the best candidate in f32 and re-evaluating
with the original CUDA sampling kernel gives 2.1849130007467465e-6 m/s.
Thus this failure is actual representation infeasibility, not iteration
exhaustion or the already-corrected zero-free certificate authorization.

The observed last band conversion, snapshot 189 after assembly 18, changes the
local obstacle pattern at j49/k334:338 from [0,0,0,0,0] to [1,0,0,1,1]. The
following prospective ledger loses adjacent tangential support. At witnesses,
the weighted support barycenter is y=.21140624582767487, z=2.0999999046325684 m;
the raw points have y=.21000005304813385 and z=2.0976853370666504 /
2.1041669845581055 m. Deleted-support renormalization therefore does not preserve
the spatial first moment. It is currently allowed by the sampling contract;
this audit does not silently change that contract or its obstacle ownership.

Evidence: actual_matrix_audit_r01/actual_feasibility.json, collective_rows.npz,
hard_only_axis_2.npz, affine_support_diagnostic.json, topology_window_summary.json,
accepted_time_audit_r02/audit_report.json and the original failure/observer files.
The matrix audit runs zero physical steps. Its core/MAC identities are
cda0eca0c0d7ca06a20ffc0bc8605b688a46511d768bde07efad268840723f63 and
cc5d2601ac4d016ee6c6f8da141740fe15d8f215fe7f092804592a724a746947.

The next representation candidate must retain all original physical points,
targets and equations, legal ownership, pressure topology and full physical-time
contracts. The current 54-per-side guard requires spacing<=1.05 dz: fixed112
config preflight passes 4x96x400 and rejects both 4x96x576 and 4x96x864. Neither
rejected candidate has been run on CUDA, and no coverage guard has been removed.
The prepared post-band remesh script now includes all actual config validators.
If continuous segment geometry is to replace the older point-density guard,
its end coverage and all downstream contracts need explicit evidence first.
No fresh8, component requalification or formal FSI1/2/3 pass is established.


## 2026-09-06: isolated band-ordering control r01

The reviewed, source-matched private-owner prototype
`topology_fixed_point_r09_20260906_01/run_topology_fixed_point.py`
(`f0bf5f44b9994834289af4b1f583ff924e75427b4f2cb5235ad6d7a8fbb10655`)
ran on strict CUDA/f32 for 207.215 s and stopped at `RED_CONTROL_MISMATCH`.
It reproduced the r09 closure exception and its `certificate_count=5`, but
the comparison against the separately instantiated matrix audit differed in
44 weights (maximum `1.1920928955078125e-7`) and 12 RHS values (maximum
`4.326996660281024e-12`). No candidate band sweep or physical step ran.

The subsequent pure-host audit verified all 18 kernel input arrays, including
grid coordinates, widths, marker state, prospective velocity, and masks, as
identical in dtype, shape, and bytes. All 14 captured window checks and 43
initialized-input preservation checks passed. The explicitly recorded absence
of the previous canonical HIBM ledger in the reconstructed owner is separate
from the strict comparison failure; it is not an atomic-commit failure.

Rows 254/257 remain identical to the original reference in all seven stored
row arrays. They have the same two H supports with weights 0.5, no free support,
and different RHS values. Their normalized dual still has `A^T dual = 0` and
the unchanged lower bound `2.184912935710554e-6 m/s > 1e-6 m/s`.
Evidence: `r01/input_identity_audit_r02.json`, SHA-256
`363cae78895359117a686be5a2ee297641ada87a18d0d71c81c0d5ccb79fdda1`,
under the external `resume_live_20260905_01` directory. The earlier
`input_identity_audit.json` is retained and superseded because its dual dot
product used NumPy float32 promotion; r02 explicitly uses host float64.

The installed Taichi 1.7.4 template key includes field object identity.
The old matrix audit reconstructed independent fields while the prototype used
native owner fields, so their compiled-kernel identities were not controlled.
This establishes a comparison confounder, not the precise cause of the observed
rounding differences. See the installed-source export
`taichi_template_identity_source.json` and the official
[kernel instantiation documentation](https://docs.taichi-lang.org/docs/compilation).

The next bounded diagnostic will compare reference A, reconstructed B, then A
again with one fixed private field set and one audit operator. Input/scalar and
output comparisons remain strict; the native capture and final committed H/global
measurements, physical markers, tolerances, and band budget remain unchanged.
The full-state r10 capture is prepared but unrun. This control result does not
prove a topology-ordering repair or improve formal FSI1/FSI2/FSI3 acceptance.
The latest actual coupled result remains r09: one accepted step to t=0.005 s,
followed by the certified step-2 failure.


## 2026-09-06: saturated band controls and production ordering

The r02 private reconstructed-owner control passed on strict CUDA with unchanged
old core `cda0eca0c0d7ca06a20ffc0bc8605b688a46511d768bde07efad268840723f63`
and fluid `fe841f4cd61159a3024820de3ddabf8be92a4ddb57c99ec5b026febec199eb14`.
Its shared-field reference A / native-input B / repeated A comparisons preserve
all 18 input arrays, four scalar identities and seven output arrays strictly.
The original exception and old rows 254/257 remain exact; their normalized dual
still certifies the old H-only lower bound `2.184912935710554e-6 m/s`.

Continuing the same frozen band phase added 92 excluded cells, then zero:
2444 became 2536. The finalized 336 physical rows comprise 290 hard-only rows
and 46 rows with free support. Old witness rows 254/257 now have legal free
DOFs. The old infeasible H-only system has not been solved; the saturated
geometry changes the legal support used for its unchanged physical equations.
Native final committed f32 measurements give H `8.992282687358966e-7 m/s` and
global `4.3388867197791114e-5 m/s`, passing the unchanged `1e-6` / `1e-4` gates.
All 112 raw positions and targets remain identical. This private reconstruction
has missing complete capture state, unqualified intermediate marker health,
and zero physical steps or accepted-time advancement.

The reviewed ordering change is now applied in production core
`c989f6ba0342d53fcd345a40454bf17f8add43d77222fa00349df7a1ee2afc23` and fluid
`a1a3324bad2ef98e83c10e144bb368625381fbf3d9e00058dc492525d3fe9234`.
`_assemble_hibm_velocity_component_face_ledger` reuses geometry-only assembly
and the raw pressure-hard refresh while closure is pending. The shared band
helper permits full closure/sealing only after a zero increment and fails closed
on cap exhaustion. Initial/air loops retain cap 8; post-solid retains 1+8 as
cap 9, plus the existing conditional disconnected cap 8. Pending blocks full
prepare/seal, public consumers and trial save; invalidate preserves it, while
successful full row clear or complete trial restore releases it.

Corrected guard RED r02 recorded six expected failures and one pass on old
source; five failures reached explicit pending dispatch sentinels and the sixth
showed the missing rejection. The new 22 host tests pass. The final combined
checks cover 45 distinct host tests: 39 combined in .667 s (including those 22),
two renamed-entrypoint checks in .025 s, and four routing checks in 1.12 s.
Ruff F/E9 passes for all changed Python files. The authority fixture repair only
supplied missing host fields; its setter and reachability implementations were
unchanged, as recorded in external `authority_host_fixture_repair.json`.

The production-helper probe
`topology_fixed_point_r09_20260906_01/run_topology_fixed_point_production.py`,
SHA-256 `702d8bae3d5c6f6639ecebb7c8a3d52f9c916abd84a5af1e6d7fb906446707e1`,
then completed r03 with exit 0 in 58.056119 s and status
`PASS_PRODUCTION_ORDERING_FROZEN_POSE`. It reproduced +92/0 and the native final
H/global measurements above, with raw state and source unchanged, no capture
errors and zero physical time. Its actual production assembly states were:

| Ledger generation | Marker closure | Pending | Sealed |
| --- | --- | --- | --- |
| 8 | disabled | true | false |
| 10 | disabled | true | false |
| 12 | enabled | false | true |

Evidence is under external `resume_live_20260905_01`: the prototype's
`r02/process.json`, `r02/red_control.json`, `r03/process.json`, and the new
`band_ordering_host_green_r01` report/log under
`topology_fixed_point_r09_20260906_01`. The corrected old-source guard file is
preserved there as `band_ordering_guard_red_r02_frozen.py`.

These results establish the bounded reconstructed production-helper check.
The subsequent from-zero r11 diagnostic completed with exit 1 in 207.155688 s:
four accepted steps reach t=.020 s, then step 5 rejects two geometry
`prepare_pair_arbitration` conflicts. The first reported witness is face
`(1,50,304)`, axis 0, path 0, authors 20304/19904. Both report registered segment
54/55 at endpoint weights (1,0), a coincident boundary point and the same
serialized target `(0, -2.892273869292694e-6, 6.691774615319446e-5) m/s`.
The cause of this new arbitration failure remains under investigation.

The r11 accepted-record/time audit passes with the original candidate and
native-record validators for all four accepted records. All 356 CSV values
(4 x 89) match exactly; all 4 x 34 native fields match in dtype, shape and decoded
byte hash. Fluid and solid each consume .005 s per accepted step and .020 s in
total, with zero remaining time. Each accepted step has one coupling trial and
no rejection; these counters exclude unaccepted step-5 work. All 193 recorded
solver source hashes are unchanged before and after the run. This host audit
executes zero physical steps and does not import Taichi or solver modules.
Evidence is in the external `hard_closure_diagnostic_5dcba96__r11` directory:
`accepted_time_audit_r01/audit_report.json` and the run's
`arbitration_source_before.json` / `arbitration_source_after.json`.

Runtime reports restored fluid, solid, marker interface and marker pressure
gradient state and no capture errors; derived search/boundary state requires
rebuilding. A full post-rollback state comparison remains pending. Historical
r09 still records its one accepted step to t=.005 s and certified step-2 failure.
Source-matched component requalification and formal FSI1/FSI2/FSI3 acceptance
remain outstanding. No threshold, physical marker target or accepted-time
contract has been relaxed.

## 2026-09-06: exact r12 geometry replay and terminal-path diagnosis

The from-zero r12 capture repeats four accepted steps to t=.020 s and the
step-5 arbitration failure, with unchanged 193-file source identity. The
original candidate/native accepted-record audit passes all 356 CSV values and
4x34 native fields, with fluid and solid each consuming .005 s per accepted
step. r12's f32 velocities differ slightly from r11; the two runs are not
claimed to be byte-identical. Full post-rollback state comparison is pending.

All 207 captured arrays (27,199,652 bytes) pass hash/dtype/shape audit. Capture
occurs after canonical transaction cleanup, before physical trial rollback;
therefore pair/cache fields in that capture are cleared. A private native
assembly replay restores 40 input fields and exactly reproduces the original
failure while observing the actual pre-cleanup cache. All input bytes, complete
runtime identity and 193 source hashes match. It advances zero physical time
and does not reconstruct complete simulation state.

The true first pair is direct author 20304 and shadow author 19904, both routed
from storage `(0,50,304)` to face `(1,50,304)`, axis 0. They share segment 54/55,
endpoint weights (1,0), nearest marker 54 and the raw endpoint target. A native
same-storage A/B/A observation proves all common provenance checks and finite
primitive projection are valid, but endpoint_clamped=1 and the same-storage
equal-axis/base validity remains zero. The support ratio is
`.037375420331954956`; marker 54 has terminal incidence degree one. Merely
removing the clamp gate would leave an inconsistent chord normal and endpoint
distance check, so the repair must use the existing terminal tangent extension.

The external staged patch `same_storage_endpoint_patch_r01/core.patch` has
Astra/max code review acceptance and passes Ruff F/E9. It preserves interior
rules, direct probe margin, mode12, raw endpoint velocity and all live identity
checks; both callers carry actual clamp diagnostics. A 13.215553-s native
original/staged/original helper probe on the captured inputs passes: original
returns remain byte-identical, staged admission/full=1/1, clamp/ratio matches
the original primitive, and independent analytic boundary error is 2.611e-8 m.
All 11 input groups and 193 source hashes are unchanged. This patch is still
external at this checkpoint; whole-route RED/GREEN and repaired assembly replay
remain pending. The helper result is not a numerical or formal acceptance.

Detailed status and exact paths are in
[the current continuation record](../refactoring/TUREK_HRON_FSI123_TRACE_REPAIR_THREAD_HANDOFF_2026-09-05.md).

## 2026-09-06: repaired r12 native geometry assembly passes

The reviewed terminal patch is now applied at core `ad7b570e`. External
`resume_live_20260905_01/repaired_assembly_r12_r01.json` records exit 0 after
448.584921 s; `repaired_assembly_r12_01/process.json` records
`PASS_REPAIRED_CAPTURED_INPUT_ASSEMBLY`. The 193-file source comparison permits
only the reviewed core change from r12; source, execution and complete strict
CUDA runtime identities remain unchanged during the replay.

All 40 actual argument/search/base-boundary/marker input fields match the
captured hashes, dtypes and shapes. All 32 non-output fields remain byte-identical.
The eight canonical output files pass independent rehashing; the original face
(1,50,304), axis0 has active/hard/owned mask3, external mask0, mobility0,
enforcement1 and region101. Its committed target 2.2249914763960987e-6 m/s
matches the native precommit float32 bits. The pair retains mode12, author
keys20304/19904, kinds0/1, endpoint clamp1 and ratio.037375420331954956.
There are 1180 owned hard components, 695 active storage rows, 1234 actual
sample evaluations and 14 endpoint clamps. All native claim-conflict and
ledger-health violation counters are zero. Astra/max independently verified
the recorded source/runtime identity and all eight output files.

This restores one private native geometry assembly, including reconstruction,
resampling and canonical commit. Marker closure is disabled; the authority is
unregistered/unsealed. It neither reconstructs complete simulation state nor
advances physical time. Coupled and formal acceptance remain pending.

The old-core toy test attempted eight endpoint/slot cases but mixed target-path
and auxiliary Y/Z failures, so it is not a clean RED. The subsequent
`same_storage_endpoint_green_r01` test was deliberately stopped after review
found identical direct and relocated sample positions in the fixture;
the owned child exited -15 after 81.517303 s. No completed test or GREEN is
claimed. Corrected whole-route tests are under independent review. The next
from-zero r13 wrapper changes only the r12 label/attempt. Its pure-host
preflight passes with 112 markers and the same numerical gates; the diagnostic
acceptance changes only expected steps1600->8 and solid-substeps100->200.

## 2026-09-06: five-step r13 prefix and systematic failure analysis

The current core is `ad7b570e`. From-zero fixed112 diagnostic r13
accepted five steps to `t=.025 s`, then rejected step 6 at `t=.030 s` during
geometry arbitration. The first conflict is face `(0,50,305)`, axis 2, with
three claims and two reported direct authors 20304/20305 on segment54/55.
The global counter is 28 target-conflict reports; it is not a verified count
of distinct faces. The live identity of the third author remains uncaptured.
All five accepted candidate/native records pass revalidation: 5x89 CSV values
and 5x34 native fields match, and fluid and solid each consume .005 s per step,
.025 s total, with zero remaining time. Each accepted step has one trial and
no rejection; those counters exclude rejected step-6 work. All 193 diagnostic
source hashes remain unchanged. The process exited1 after817.290128 s, with
runtime-reported physical rollback and no capture errors. A full independent
post-rollback state comparison remains outstanding.

The terminal patch's actual r12 native assembly passes, and 18 distinct focused
tests have passed across bounded batches. The final two-line shared-coordinate
fixture cleanup was verified by the ordered roundoff/cap-slot0/cap-slot1 trio;
the complete18 were not rerun as one batch on that test-only cleanup. Neither
focused evidence nor the five-step prefix qualifies the eight-step diagnostic
or any formal FSI1/FSI2/FSI3 stage.

The user requested systematic analysis and repair after the repeated short
prefixes. No new coupled trial is being launched. Current work audits complete
source membership, geometry ownership, canonical reconstruction and atomic
publication across frozen states. The existing non-interpolated full-source
assembler is being assessed for reusable contracts; its interpolation guard
has not been removed. Thresholds and all112 physical marker positions/targets
remain unchanged.

Focused endpoint validation is complete with a precise multi-batch boundary:
the clamped known-solution method passes4 endpoint/slot cases; the remaining17
methods have15 passes and two old cap-fixture failures. The failures follow a
prior fixture's leaked nonuniform X coordinates, independently confirmed from
the exact live center values and fixture lifecycle. A two-line addCleanup in
the producer restores those coordinates. The ordered roundoff/cap0/cap1 trio
then passes3/3 in27.039s. The18 distinct methods cover exact closed endpoints,
10 invalid terminal-proof cases, direct-shadow selection, roundoff, topology,
storage/route ownership and atomic cleanup. Final tests SHA:f5c9c66f; production
source remainsad7b570e. There is no claim of18 tests in one final-source batch.

The r13 accepted-time audit executes no Taichi/solver work and adds no physical
time. Its exact5-step CSV/native/candidate checks pass and leave all source and
input bytes unchanged. The process's original exception and rollback report
are preserved. Its failed prefix is not a restart checkpoint.

The pure-host capture audit verifies all207 NPY files (27,226,148bytes), with
SHA/shape/dtype checks and14 cross-checkable live-window arrays equal. The old
observer targets a fixed `(0,50,336)/axis1` window: its regex only recognizes
`first_face=... axis=...`, while current native errors expose dictionary keys
`component_face` and `component_axis`. The later input observer runs after
transaction cleanup, so the actual first-face cache is cleared. Consequently
the exact third source and the unique set of failed faces cannot be recovered
from this capture alone. No values are fabricated for these missing fields.

The captured window contains540 face-axis lanes:432 count0,30 count1,73 count2
and5 count4. Those count4/mode20 lanes are the supported inactive-axis extrusion
cohort, not five proven failures. Target conflict reports can increment in the
author scan, delayed-pair restoration and final cardinality checks (current
core22673,22942,23052);28 is a report-event count, not a unique-face guarantee.

The systemic review distinguishes certified fixed-system infeasibility, solid
band/support ordering, and geometry/source arbitration. It does not assume one
universal cause. For interpolated geometry, precompute18442ff and prepare23020ff
choose/check different author shapes; the existing full-source assembler is
enabled only at27526ff with interpolate_flag==0. This is a specific contract
coverage risk to investigate. No alternate assembler has been enabled and no
new coupled trial has been launched after the user's steering.

The next evidence is a generic full-domain frozen-assembly observer, with the
known r12 pass and r13 original failure as controls, preserving actual inputs,
raw physical targets, source/runtime identity and the single commit boundary.
The fix must be validated across source-order/cardinality, topology/endpoint
transitions and genuine conflicts before continuous integration resumes.

The separate identity audit verifies current component15 digest985784ab and
formal206 digest497ee748; diagnostic193 captures have a different scope. All10
historical component manifests are stale under their shared15-file identity.
No formal run or component requalification is inferred from this audit, and the
fixed1124x96x400/solid200/8-step diagnostic remains distinct from registered
S0's4x48x288/automatic112/solid100/1600steps. No formal/config gate changed.


## Full-domain native cohort diagnosis, 2026-09-06

The unchanged193-source r13 replay completed in31.166811s with
`PASS_EXPECTED_NATIVE_REJECTION` and expected exit1; the exact original
RuntimeError is preserved. All40 inputs, including eight canonical fields,
remain byte-identical. Four149-array snapshots cover precompute, prepare,
reconstruction and actual precommit before cleanup. Prepare already contains
all12 distinct negative-witness face-axis lanes; reconstruction adds none.
The28 target-conflict events are not28 distinct faces. Of35 count>2 cohorts,
27 are legal count4/axis0/mode20 controls. A count>2 failure classifier is wrong.

| Face at x0, y50, axis2 | Actual members | Cached geometry seeds |
| --- | --- | --- |
| z305 | D20304/D20305/S19905, all [54,55] | D20305/S19905 |
| z306 | D20305 [54,55]; D20306/S19906 [55,56] | D20306/S19906 |
| z347 | D20346 [94,95]; S19947 [95,96] | D20346/D20347 |

The same pattern appears at x1..3. A non-member may provide a geometry witness,
but it must not impersonate a real claimant or replace any real shadow in the
certificate. Same primitive, same region or similar velocity alone is not proof
that all members share a supported, uniquely owned physical trace.

The r12 success control completed in606.338727s with
`PASS_R12_FROZEN_ASSEMBLY_CONTROL`, exit0 and32 non-output inputs unchanged.
Five149-array snapshots preserve both real precommit calls; use the last one
for the eight published targets. It contains35 legal count4 cohorts and zero
negative-witness faces. Capture/audit errors are empty in both controls;
all original207 input files and the source/runtime identities match.
These are one-assembly diagnostics, zero physical steps and zero accepted time.

The primary agent independently scanned all109 registered segments in F64.
The nearest edges are [54,55], [55,56] and [94,95] respectively; their squared
distance gaps to the next edge are1.206589926e-5,7.708527504e-6 and
5.370475264e-8m2. Cached B differs by at most3.916e-8m and U_B by1.185e-12m/s
from those nearest projections. This is a host-only distance audit, not a
native member, source-support or interpolation certificate.

Independent Astra/max read-only review accepts separation of geometry seeds
and actual members and requires a complete local registered C0 trace proof,
including adjacent primitives. It does not approve a count-only exception,
an unconditional full-source/non-interpolated switch, or a band bypass.
The DD and DS helpers derive nominal Q from different source-margin rules;
source/seed substitutions must establish the intended common B/N/Q/target.

The approved diagnostic query proposal contains28 real pair-helper queries,
ten per-face author/anchor checks and20 finite-projection observations.
Three original seeds must reproduce byte for byte before alternative queries.
Executable preparation and native execution are pending at this checkpoint.
No solver implementation or test changed during this systemic observation.
Before another coupled run, implement and verify the common certificate,
source-order/cardinality/geometric transitions, current endpoint controls,
invalid third sources and other true conflicts, and the atomic eight-field
publication boundary. Physical marker targets, thresholds and formal order
remain unchanged.

Evidence beneath the existing external robustness root:
- `resume_live_20260905_01/systemic_frozen_cohort_r01/native_r13_r01`;
- `resume_live_20260905_01/systemic_frozen_cohort_r01/native_r12_r01`;
- combined host analysis `systemic_frozen_cohort_r01/analysis_native_pair_r01`
  SHA`d885891edaafacaf57f09e5674f6c5bdfbfe504d0110228fdfcc604704eed342`;
- independent host owner report `systemic_registered_owner_host_r01/report.json`
  SHA`2f8d82f9e58e4a91cc7c59f2eb0b33156903890ad9102c6570abfffc1da814c7`.

## Common face-trace implementation and controlled native comparison, 2026-09-06

The reviewed common-cohort repair is applied at core `d8a14f64`.
Three unchanged compact fixtures covering r13 z305/z306/z347 now pass strict
CUDA/f32 assembly and the existing affine known-solution check. The original
core rejected all three in558.449204s; the candidate passes all three in
1133.775381s, including cold compilation. The highest affine error is
1.49011612e-8m/s. This comparison measures correctness, not acceleration.
All193 source hashes and full runtime identity stay fixed within each run;
only core.py differs between runs. Fixture bytes, cached geometry/routes and
original/shadow sample payload observations are identical.

The fix separates cached geometry seeds from actual consumed authors. A new
path can replace an existing rejection only after every actual member and
the cached seeds pass current source/storage, registered-owner and geometric
support checks. It preserves the cached B/N/Q, canonical sample formula and
single atomic publication. Failed proof retains the original rejection
events; no global health counter is reset to admit a cohort. The new temporary
owner/mask fields are included in native commit and error cleanup.

The integrated positive/invalid-source/rollback and existing-path suite is
being reviewed. Candidate-source full-domain r13 and r12 success replays are
pending. The last coupled result remains the original ad7b570e r13 prefix:
five accepted steps to t=.025s, followed by a step-6 rejection at t=.030s.
The28 events were traced to12 distinct face-axis lanes, including two- and
three-author cohorts. Fluid and solid accepted time each equals .025s in
that prefix; a full independent physical post-rollback comparison remains
outstanding. No new coupled run, component qualification or formal
FSI1/FSI2/FSI3 pass is implied by these zero-time operator regressions.
The112 physical marker obligations, numerical thresholds and formal order
are unchanged.

Native transport observation r03 validates25 alternative original-to-storage
proofs without failures;18/28 complete queries are geometrically valid and
the other10 reject unsupported geometry. This observation preserved all40
inputs and140 query fields, all193 source hashes and runtime identity. Cached
normals are retained because small transported-normal differences can cross
the existing inactive-component threshold; no new physical tolerance is added.

Independent Astra/max review accepted the core diff and the mechanical
extraction of the existing trace/target publication calculations. Root static
checks and Ruff E9/F pass. These reviews do not substitute for the pending
integrated negative controls, full frozen ensemble or coupled validation.

Evidence: `resume_live_20260905_01/common_cohort_core_draft_r01`,
`systemic_geometry_binding_probe_r03/native_r01`,
`systemic_cohort_regression_draft_r03/native_red_r01`,
`systemic_cohort_postfix_probe_r01/native_green_r01`, and
`systemic_cohort_postfix_audit_r01.json` under the same resume artifact root.

Recomputed component15 digest `b36f3e27f7d864d0624ec0d5a0d768acbe6c12a976df09dbe25fc00841f399c0`;
formal206 digest `f6c56abbbab4d12ac7806e96e8c2a676ec8784c4807ef582c6502b91b1f33d1a`. The10 historical
component manifests still require current-source qualification.


## Full-domain common-cohort controls (2026-09-06)

Core `d8a14f64` passes all10 new and3 existing strict-CUDA contracts in
792.493602486s. The seven negative cases compare complete C8 outputs before
fixture reset; the bad additional-source case separately reaches the native
arbitration failure. No numerical threshold is changed.

The reviewed external `systemic_frozen_cohort_r02` replays the original207
array captures and restores the same40 real inputs. Its inventory has144
owner fields plus9 actual arguments:153 arrays in each of6 snapshots.

R13 returns `PASS_R13_FROZEN_COMMON_TRACE_SUCCESS`. All12 lanes at x0..3,
y50,z305/306/347,axis2 use exactly mode256. Seed masks12/12/5, consumed
masks13/13/9 and actual counts3/3/2 are distinguished from proved masks13/15/13,
which may include unused valid sources. Owners are[54,55,-1]/[55,56,-1]/
[94,95,-1]. All14 old cache payloads are byte-exact to immutable old precompute;
all15 including owners persist through prepare, reconstruction and both
precommits. Reconstructed alpha lies in[0.4759862720966339,0.476005882024765].
All global conflicts are zero.

R12 returns `PASS_R12_FROZEN_SUCCESS_UNCHANGED_OUTPUTS`. It retains endpoint
face(1,50,304),axis0,mode12,keys/kinds and clamp; it never enters common mode.
All8 final arrays equal the immutable old native success byte for byte.
Both runs satisfy C8 publication, preserve32 non-output inputs, clear39 real
temporary fields and retain source193,dependencies,captures and full CUDA
runtime through return. Root checked all6 snapshot and8 published-file hashes.

Execution manifest SHA:
`fc5c00d83b06ef8a320a7214489354e555dfca4c6f044d2743da82563bb7f4a5`.
R13 process SHA:
`01a90808c2363c2b4bfdd017d8ce65bb21a16003676260eff6761dc764a51d71`.
R12 process SHA:
`2562afca4a87731d6fd2eef6c6c832e3b7b8bd11272de19e43c26d57124103b0`.
The packet contains `native_r13_r01_artifact_audit.json` and
`native_r12_r01_artifact_audit.json`. Owned times1198.438568592/39.114366293s
have different compile/cache conditions and cannot establish acceleration.

Both advance zero physical steps/time. Fresh coupled validation must still
cross the original step6 stop with unchanged numerical and accepted-time
gates. Component15/formal206 digests remain b36f3e27/f6c56abb; these controls
supply neither historical component qualification nor formal clean-source entry.

## R14 systemic selector disagreement and common-face route repair (2026-09-06)

The from-zero `hard_closure_diagnostic_5dcba96__r14` run on core `d8a14f64`
accepts five steps through t = 0.025 s; step 6 at trial t = 0.030 s rejects
during assembly 83 with 56 common-trace reconstruction conflicts. Fluid and
solid accepted time each totals 0.025 s. A complete independent physical
post-rollback equality check remains outstanding; the prefix does not prove it.

The original failure observer could not serialize its native snapshot because
a path-only capture wrapper intercepted `np.savez_compressed` and rejected
the observer's `BufferedWriter` output stream. The repaired external observer
binds the original writer and reruns the immutable rejected input without
advancing time. All 18 native stages, 153 arrays per stage (2,754 readbacks),
pass artifact checks. Prepare admits 76 exact mode256 common cohorts and
reconstruction newly rejects 56. All 56 have D/S cached seeds and report
path2 / claim_count3. The first rejected lane is (0,50,305), axis2, with
actual positive keys20304/20305, cached keys20305/19905, and owner[54,55,-1].
Cached pair state remains unchanged through reconstruction, all eight original
canonical outputs remain unchanged, and all 39 error-cleanup fields are neutral.
These are observed native-state facts; not every common-entry precondition was
independently queried.

An isolated strict-CUDA selector query at this first lane finds a valid scalar
primary face(0,50,306) and a valid pair face(0,50,305). Only the primary-face
mismatch rejects the queried cached-target subpath: its other observed geometry,
alpha and finite-target rejection flags are zero. The query forces entry validity
for that subpath, does not independently certify full common admission, and
publishes nothing. Its result explains the route disagreement; the following
native compact regression separately exercises the admission/publication path.

The immutable `tests/solvers/fixtures/turek_hron_common_trace_r14_tilted.json`
retains the physical tilted geometry and records the original face and route
provenance. Its affine manufactured target checks the same native operator.
On `d8a14f64`, the new case returns `CONFIRMED_R14_COMMON_ROUTE_RED`
after 1179.790855181 s, with exact path2 / claim_count3. All eight canonical
arrays remain unchanged before reset; four common scratch fields and all nine
coordinate fields are restored.

The reviewed repair at core `a50b67f0` changes only
`_reconstruct_canonical_component_face_common_trace` and its call in
`_reconstruct_velocity_dirichlet_component_face_segment_claims_kernel`.
Every admitted common trace uses the existing pair route for its certified
physical face. Removing the D/D-versus-D/S selector split also removes the
unused projection-marker argument and second-author temporary. The cached
B/N/Q and actual-member proof, owner/mask state, route/alpha/geometry checks,
canonical target equation, error cleanup and atomic publication are unchanged.

The strict-CUDA/f32 suite returns `PASS_FOURTEEN_FOCUSED_ROUTE_CONTRACTS`
in 1231.187055591 s: the new tilted affine case plus the previous 13 contracts.
All 14 case hashes match, with no skips, errors or audit failures. Source193 and
Taichi1.7.4/f32 runtime identities match before and after. RED/GREEN advance zero
physical time; their compilation/cache-inclusive durations imply no acceleration.

Repaired full-domain R12/R13/R14 controls in `systemic_pair_route_replay_r01`
pass their required native results and complete artifact readbacks, as recorded
below. Fresh R15 used fixed112, 4x96x400, solid200, dt0.005 and eight requested
steps from zero. It then failed at step 6 after five accepted steps; its
reviewed host-only auditor has now validated the accepted prefix at 0.025 s
for each subsystem. This terminal audit passes, while fresh8 fails.
Component qualification, formal FSI1/2/3 acceptance and independent full physical
rollback equality remain unverified. Physical obligations and thresholds are unchanged.

Current `source_identity_boundaries_r03.json` recomputes component15 SHA
`f4a5891bb7d036610bc706351202707fab968fac9c98cd8587761354eaad604b`
and formal206 SHA
`3693bf856569ccc068f1dcec2a7a48f6e718a7444724f19ec7d194805b017037`.
All 10 historical component manifests remain stale in the same four paths:
case, core, MAC and fluid. Diagnostic193, component15 and formal206 are distinct
source contracts; the dirty tree still fails formal clean-source entry.

Evidence under `resume_live_20260905_01`:
`systemic_r14_frozen_rejection_r01/native_r14_r01/root_audit_r01.json`,
`r14_canonical_selector_probe_r01/native_r01/query_report.json`, and
`r14_common_route_contracts_r01/native_red_r01/process.json` /
`native_green_r01/process.json`. The RED process SHA is
`9a31c1421074b3390baf68afec83fc90dbfba150492ba96e838ec08b89f629bc`;
the GREEN process SHA is
`84d250d3a4cabd30d5cad288c3893b77fe5e01ee11453c54b7c92137523bdd66`.

## Completed repaired frozen controls and terminal R15 (2026-09-06)

All three controls on `a50b67f0` exit 0 and pass
`PASS_COMPLETE_NATIVE_ARTIFACT_READBACK`. Each contains 21 snapshots of
153 arrays (3,213 checked arrays), with 193 source and 747 pinned files verified.
All 32 non-output inputs are unchanged, all eight final outputs satisfy native
publication, and all 39 temporary fields are neutral after native cleanup.

| Replay | Required native result | Owned wall time (s) |
| --- | --- | --- |
| R12 | `PASS_R12_FROZEN_SUCCESS_UNCHANGED_OUTPUTS` | 483.8980868 |
| R13 | `PASS_R13_FROZEN_COMMON_TRACE_SUCCESS` | 56.6939531 |
| R14 | `PASS_R14_FROZEN_COMMON_ROUTE_SUCCESS` | 57.8818058 |

R12 keeps its original endpoint control at face(1,50,304), axis0, never enters
common mode, and all eight arrays are byte-exact to its immutable old success.
R13 keeps 12 common cohorts and all 15 cache payloads through reconstruction and
both precommits. R14 keeps all 76 common cohorts, all 15 full-domain cache arrays,
prepared certificates and positive author keys unchanged; reconstructed alpha
and target pass their checks, and the 56 former conflicts are absent.
Source193, host numerics and strict CUDA/f32 runtime match before and after.

The wall times come from the completed owner records; they include compilation,
cache and process overhead and establish no acceleration. All three controls
advance zero physical time and reconstruct only the native assembly inputs,
not a complete physical restart state. They do not provide component or formal
FSI1/2/3 qualification, or independent full physical rollback equality.

Frozen replay execution manifest SHA:
`522616b78d1eac279e629d656d71a9c780df0dd4b64b18b8f2bdc9d9e86106cb`.
Each result is paired with its `root_artifact_readback.json` in
`systemic_pair_route_replay_r01/native_r12_r01`, `native_r13_r01` or
`native_r14_r01`; their exact process and readback hashes follow below.

| Replay | process.json SHA-256 | root_artifact_readback.json SHA-256 |
| --- | --- | --- |
| R12 | `e59e227c142554f067d1d4441413bb0a212f1760c2e3c18bfa029cca70e078d1` | `c948bcfb219454b8c5ecbe138d1c2aa3d2c94964bc682ce3d628b04efe29c85a` |
| R13 | `a88f079b57ada221222941552547060799b4a14b4c08414cdb0e7062440f865e` | `43352f2474308dd71fbec55eb4bde16b4139d6924b840c547ce86634afbf732c` |
| R14 | `cce523ed53cedafee11713268b978207dc2a71654b3beecdda1e600e5d9c0386` | `480e27a6b578a478390ea252ba18a9710faf6e732cac5f877c587aa3fde56f8b` |

R15 terminated under owner record `fresh96_common_route_r15.json`:
PID/PGID 391, start Unix 1788687465.7436998, end Unix 1788689126.4022677,
exit 1; the owning tool session was 34446. The owner record SHA is
`5ff2a34635897e4f4b1ca42446bac3492f457498d16dbfdab59e7baab4bab66d`.
Its output is `hard_closure_diagnostic_5dcba96__r15` under the external
`robustness_work` root. Execution manifest
`fresh96_r15_preparation/execution_manifest.json` SHA:
`0b4d21ddd21facf317882d1ed5d116643c41bc3f00ed21128e18d866b564c6fa`.

The run used fixed112, 4x96x400, solid200, dt0.005 and eight requested steps
from zero. Five were accepted at t = 0.005, 0.010, 0.015, 0.020 and 0.025 s.
Step 6 failed at trial t = 0.030 s, during assembly 99. Native precommit
reported four target conflicts from `prepare_pair_arbitration`; the first
face is (0, 50, 342), axis 2, path 0, claim_count 2. No root cause is inferred
from this signature alone. The full capture contains 144 owner arrays plus
nine argument arrays, taken before native cleanup, with no capture errors.
The separate input manifest contains 207 fields and is complete.
`r15_execution_audit.json` reports `WRAPPER_RAISED_ORIGINAL_EXCEPTION`,
zero audit errors, and unchanged Source193 and 25 dependency maps.

The host-only terminal auditor passes with
`run_outcome=FAILED_WITH_VALIDATED_ACCEPTED_PREFIX`:
all five original candidate and native-record validators pass, with 89
CSV fields matched per record. Accepted fluid and solid time each totals
0.025 s. `fresh8_diagnostic_passed=false` and `formal_benchmark_pass=false`.
The reported rollback outcome is `restored`, with derived search/boundary
state requiring rebuild; `rollback_full_state_independently_verified=false`.
The audit executes zero physical steps and imports no Taichi or solver.
Accepted-prefix work counters exclude the final unaccepted trial.
The capture is diagnostic evidence, not a restart checkpoint.

The initial host audit `accepted_time_audit_r01` failed the authoritative-cwd
guard before auditing and remains preserved. The corrected-cwd
`accepted_time_audit_r02/audit_report.json` is the passing failed-prefix audit.
Its pass does not imply that R15 completed or that continuous failures are fixed.

| R15 artifact | SHA-256 |
| --- | --- |
| `r15_execution_audit.json` | `6885814b00492a80695c4c874a22c5d06ed8240c5fec5f17ed58fcb5dd737244` |
| `precommit_failure_capture.json` | `ed04e16ff4cb6601e45a9b686cc32ffc49437d0e7ab05114580924ed614c91ac` |
| `failed_assembly_input_manifest.json` | `07f7a6c538276d91704d6e0b0a613b85a131121e2c5898dffa9383d3e38a1a31` |
| `accepted_time_audit_r01/audit_report.json` (wrong cwd) | `acae76109f36acb5265151420d13e8f6de257c0f5b6c2d5a18197503dc2ac8a6` |
| `accepted_time_audit_r02/audit_report.json` (passed audit) | `f9c64204171025fb4cfbea0c669c5bad4088c67577346ce30d4b0db95377a433` |

## Source-current coarse S0 check remains unverified (2026-09-06)

The bounded read-only inspection of original R04, `frozen_trace_48x288__r01`,
and the later band/common control evidence found no recorded source-current
recheck of the original 4x48x288 step-8/call-95 obstruction. Later production
band and R12/R13/R14 controls use 4x96x400 and different poses or assembly
inputs. Their passes do not qualify this original coarse operator.

R04 preserves the failing pose after seven accepted steps, raw 112-marker
targets and solid x/v/F/rest/fixed data. It does not preserve the complete
search, canonical, pressure and physical restart state. The old coarse helper
also raises solid100 to 200, uses boundary time 0.035 s, restores the old
obstacle mask and calls a removed private assembler. It cannot be reused
unchanged for a source-current check. Its saved coefficients and dual/minimax
obstruction remain valid evidence for those historical fixed systems only.

The next bounded check requires a fresh, source-pinned zero-time adapter:
preserve the R04 pose and raw physical targets with grid 4x48x288, automatic
112 markers, solid100 and dt = 0.005 s. Rebuild current case geometry,
dynamic volume, search, protection and band topology; do not overwrite the
resulting masks with historical masks. Use the current post-solid
`_stabilize_hibm_solid_band` route with nine passes and external faces at
the failing step-8 context time, 0.040 s. The R09 post-band helper's eight-pass
budget applies to a different starting phase.

Capture the current native operator and actual committed f32 H/global residuals
with a separate audit operator, without changing the live prospective candidate.
Retain all 112 physical markers and the existing 1e-6/1e-4 m/s thresholds,
and preserve original exceptions separately from capture errors. Recompute
current rows, coefficients and topology; the old 320 hard-row count and old
dual certificate do not establish the new operator's feasibility. No fluid,
solid or coupled physical time advances in this check.

This preparation has not been implemented or executed. Even a later passing
reconstructed-pose check would not reproduce R04's complete original physical
state or qualify registered S0; coupled current-source evidence and the formal
entry gates would still be required.
