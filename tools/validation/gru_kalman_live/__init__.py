"""R25B offline preparation and live no-commit evidence helpers.

The package initializer intentionally imports no submodules so the official CUDA
runner can load ``candidate_bundle`` without importing PyTorch.
"""
